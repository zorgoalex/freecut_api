# Plan: Top-K + Business Scoring + GA Profiles

## Цель
Реализовать рефакторинг ядра оптимизации в 3 этапа:
1. Форк библиотеки и возврат top-K кандидатов.
2. Бизнес-скорер для финального выбора (hard constraints + compactness/void/perimeter).
3. Вынос GA-параметров в API-профили.

## Этап 0. Baseline
- [x] Зафиксировать baseline-метрики на `tests/fixtures/multisheet_oversized.json`.
- [x] Зафиксировать baseline на вариативном наборе `tests/fixtures/multisheet_varied_4sheets.json`.
- [x] Сохранить артефакты сравнения (report + top-10).

## Этап 1. Форк библиотеки + top-K
- [x] Подключить локальный форк `cut-optimizer-2d` через `path` dependency.
- [x] Добавить в форк новый API для получения `top_k` кандидатов популяции.
- [x] Сохранить обратную совместимость существующих `optimize_*`.
- [x] Добавить тесты форка на детерминизм и корректность `top_k`.
- [x] Запустить тесты проекта после интеграции.

## Этап 2. Бизнес-скорер
- [x] Добавить ранжирование кандидатов по бизнес-формуле в сервисе.
- [x] Применить hard constraints (`placeable_ratio == 1`, `internal_void == 0`).
- [x] Добавить телеметрию выбора победителя и причин отбраковки.
- [x] Добавить/обновить интеграционные тесты.

## Этап 3. GA-параметры в API
- [x] Добавить API-профили `fast|balanced|quality`.
- [x] Вынести `epochs/breed_factor/survival_factor` в конфиг профилей.
- [x] Добавить `ga_override` с валидацией диапазонов.
- [x] Обновить OpenAPI и тесты.

## Критерии готовности
- [x] Нет регресса по стабильности API.
- [ ] Есть улучшение разнообразия top-N и снижение повторяемости рваных пустот.
- [x] Бизнес-скорер влияет на финальный выбор кандидата предсказуемо и прозрачно.
